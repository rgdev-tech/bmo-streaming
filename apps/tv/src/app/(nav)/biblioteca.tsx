import { Pending } from '@/bmo/Pending'

export default function BibliotecaScreen() {
  return (
    <Pending
      title="Biblioteca"
      detail="Necesita sesión iniciada, y todavía no hay pantalla de login para TV. Las descargas quedan fuera: son específicas del teléfono."
    />
  )
}
